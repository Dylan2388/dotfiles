[theme]
name = "tokyonight"
auto_switch = false

[keys]
# Select/focus an agent by index (e.g. prefix then alt+1..9 focuses agent row 1..9)
focus_agent = "prefix+alt+1..9"
# Optional: cycle through agents without an index
previous_agent = "prefix+alt+p"
next_agent = "prefix+alt+n"
